#/patterns
NGINX_FLUEND %{IPORHOST:clientip} - \[%{DATA:username}\] - - \[%{HTTPDATE:timestamp_nginx}\] \"%{WORD:method} %{NOTSPACE:request} \HTTP/%{NUMBER:httpversion}" %{NUMBER:response} (?:%{NUMBER:bytes}|-) \"%{NOTSPACE:querystring}" \"%{DATA:http_user_agent}" %{NUMBER:request_length;long} %{NUMBER:response_time;double} %{DATA:upstream_proxy} %{NUMBER:upstream_bytes_sent;long} %{NUMBER:upstream_response_time} %{NUMBER:upstream_response}